#include <stdio.h>
#include <stdlib.h>
#include <math.h>
#include <time.h>
#include <string.h>

int main(){
    // Take input 
    int a, b, c;
    scanf("%d %d %d", &a, &b, &c);
    
    if (a,b,c > 0 && a == c && a == b && c == b)
    {
        printf("Yes");
    }
    else{
        printf("No");
    }

    return 0;
}