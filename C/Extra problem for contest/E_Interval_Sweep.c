#include <stdio.h>
#include <stdlib.h>
#include <math.h>
#include <time.h>
#include <string.h>

int main(){
    // Take input 
    int a, b;
    scanf("%d %d", &a , &b);
    
    // Apply Conditon  
    if (a, b >= 2 && a == b || a - b == 1 || b - a == 1)
    {
        printf("YES");
    }
    else{
        printf("NO");
    }

    return 0;
}