#include<bits/stdc++.h>
using namespace std;

int main()
{
    // Take input 
    long long int a, b, c, d;
     cin >> a >> b >> c >> d;
    // Do the oparation 
     long long int x = (a * b) - (c * d);   
    // Print the result 
     cout << "Difference = "<< x;




    return 0;
}